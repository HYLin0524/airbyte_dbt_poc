config:
  partial_parse: true
  printer_width: 120
  send_anonymous_usage_stats: false
  use_colors: true
normalize:
  outputs:
    prod:
      database: poke
      password: root
      port: 3306
      schema: poke
      server: localhost
      type: mysql
      username: root
  target: prod
